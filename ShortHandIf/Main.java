import javax.swing.text.StyledEditorKit;

public class Main {
    public static void main(String[] args) {
        int age = 18;
        String result = (age >= 18) ? "Eligible to vote" : "Note Eligible to vote";
        System.out.println(result);
    }
}